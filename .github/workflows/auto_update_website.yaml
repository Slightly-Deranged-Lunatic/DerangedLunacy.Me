name: Auto Update Website
on:
    push:
        branches: "main"

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - name: Run a one-line script
              run: git fetch https://github.com/Slightly-Deranged-Lunatic/DerangedLunacy.Me